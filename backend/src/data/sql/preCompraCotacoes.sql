-- Listagem de cotações (status 3 = Decisão de compra, 4 = Encerrada) para Pré Compra.
-- Fragmentos reutilizados em preCompraRepository.ts.

-- LIST_SELECT + BASE_JOINS (listagem paginada)
-- PDF_SELECT + PDF_JOINS (emissão PDF com contato)
