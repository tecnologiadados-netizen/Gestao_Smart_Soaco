/**
 * Fotos dos líderes: a chave deve ser o nome **exatamente** como na planilha (coluna líder).
 * O valor é a URL em `public/`, ex.: `'/lideres/ricardo.png'`.
 *
 * Exemplo:
 * `{ 'RICARDO CARVALHO PINTO': '/lideres/ricardo.png' }`
 */
export const LIDER_FOTOS: Record<string, string> = {}
